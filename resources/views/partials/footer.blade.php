{{-- <ul>
    @foreach ($listDcComics as $item)
        <li>{{ $item }}</li>
    @endforeach
</ul>

<ul>
    @foreach ($listDc as $item)
        <li>{{ $item }}</li>
    @endforeach
</ul>

<ul>
    @foreach ($sitesList as $item)
        <li>{{ $item }}</li>
    @endforeach
</ul> --}}
